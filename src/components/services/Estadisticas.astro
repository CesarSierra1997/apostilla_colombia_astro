---
const stats = [
  { label: "Apostillas realizadas", value: "+25,000", icon: "📝" },
  { label: "Traducciones oficiales", value: "+18,000", icon: "📄" },
  { label: "Homologaciones exitosas", value: 460, icon: "📘" },
  { label: "Tasa de satisfacción", value: "99.9%", icon: "👍" },
];
---

<section class="py-10 px-4" id="garantia">
  <div class="max-w-6xl mx-auto px-6">
    <div class="text-center mb-16">
      <h4 class="text-3xl md:text-3xl font-bold text-white tracking-tight">
        📈 Resultados que hablan por nosotros
      </h4>
      <p class="mt-4 text-zinc-400 text-lg max-w-2xl mx-auto">
        Casi dos décadas ayudando a personas con trámites oficiales y legales en Colombia y el mundo 🌍
      </p>
    </div>

    <div
      class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-8 text-center"
    >
      {
        stats.map((stat, i) => (
          <div
            class="stat-card flex flex-col items-center justify-center bg-zinc-800/80 rounded-2xl p-8 border border-zinc-700 shadow-xl opacity-0 translate-y-6 transition-all duration-400 ease-out hover:scale-[1.05] hover:shadow-2xl hover:border-indigo-500 hover:bg-zinc-800/90"
            style={`transition-delay: ${i * 150}ms`}
          >
            <div class="text-5xl mb-4 animate-pulse-slow">{stat.icon}</div>
            <h3 class="text-3xl font-extrabold text-white leading-tight">
              {stat.value}
            </h3>
            <p class="text-zinc-400 mt-2 text-base text-center leading-snug break-words">
              {stat.label}
            </p>
          </div>
        ))
      }
    </div>
  </div>
</section>

<script is:inline>
  document.addEventListener("DOMContentLoaded", () => {
    const cards = document.querySelectorAll(".stat-card");

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add("opacity-100", "translate-y-0");
            entry.target.classList.remove("opacity-0", "translate-y-6");
          } else {
            entry.target.classList.remove("opacity-100", "translate-y-0");
            entry.target.classList.add("opacity-0", "translate-y-6");
          }
        });
      },
      {
        threshold: 0.3,
      },
    );

    cards.forEach((card) => observer.observe(card));
  });
</script>

<style>
  @keyframes pulse-slow {
    0%,
    100% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.3);
    }
  }

  .animate-pulse-slow {
    animation: pulse-slow 5s infinite;
  }
</style>
