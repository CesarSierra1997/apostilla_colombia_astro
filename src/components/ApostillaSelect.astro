---
const apostillaOptions = [
  { label: "Apostilla de documentos personales", url: "/#apostilla-personal" },
  { label: "Apostilla de documentos académicos", url: "/#apostilla-academica" },
  { label: "Apostilla de documentos judiciales", url: "/#apostilla-judicial" },
  { label: "Apostilla para el exterior", url: "/#apostilla-exterior" },
];

let isOpen = false;
---

<div class="relative group">
  <!-- Botón principal -->
  <button
    class="flex items-center gap-1 px-2 py-2 transition hover:text-purple-500 dark:hover:text-blue-500"
    id="apostilla-toggle"
    aria-expanded={isOpen}
  >
    Apostilla
  </button>

  <!-- Dropdown -->
  <div
    class="absolute left-0 z-20 hidden w-56 p-2 mt-1 text-sm rounded-lg shadow-lg bg-gray-400 group-hover:block"
  >
    {apostillaOptions.map((item) => (
      <a
        href={item.url}
        class="block px-3 py-2 rounded-md hover:bg-purple-100 dark:hover:bg-gray-700 transition"
      >
        {item.label}
      </a>
    ))}
  </div>
</div>
